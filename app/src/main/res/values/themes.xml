<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.QRScanner" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
         <item name="colorPrimary">@color/main_color</item>
    </style>

    <style name="Theme.QRScanner" parent="Base.Theme.QRScanner" />
    <style name="BottomNavigationIconStyle" parent="">
        <item name="itemRippleColor">@android:color/transparent</item>
        <item name="itemIconTint">@color/bottom_nav_text_color_state</item>
        <item name="itemTextColor">@color/bottom_nav_text_color_state</item>
    </style>
    <style name="navStyle" parent="">
        <item name="itemRippleColor">@android:color/transparent</item>
        <item name="itemIconTint">@color/bottom_nav_text_color_state</item>
        <item name="itemTextColor">@color/bottom_nav_text_color_state</item>
    </style>
</resources>